import { PageHeader } from "@/components/page-header";

export default function BackupPage() { return <><PageHeader title="Backup & Restore" description="Use application exports plus Hostinger database backups"/><div className="grid gap-6 lg:grid-cols-2"><section className="card"><h2 className="font-semibold">Application backup</h2><p className="mt-2 text-sm text-slate-500">Downloads all financial and master data as a versioned JSON file.</p><a className="btn-primary mt-5" href="/api/backup">Download backup now</a></section><section className="card"><h2 className="font-semibold">Full database restore</h2><p className="mt-2 text-sm text-slate-500">For safety, production restore uses the supplied server script. It first creates a safety backup, imports the selected SQL file, and then you run Data Health.</p><code className="mt-4 block rounded-lg bg-slate-950 p-3 text-xs text-slate-100">bash scripts/restore-db.sh backups/your-backup.sql</code><p className="mt-3 text-xs text-amber-600">Run only from SSH after reading SETUP.md. Restore replaces database state.</p></section></div></>; }
